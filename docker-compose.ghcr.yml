# Run published release images from GHCR instead of building locally.
#
#   export MDCP_RELEASE_TAG=v0.1.0   # or latest
#   cp .env.example .env && ./scripts/generate-secrets.sh >> .env
#   docker compose -f docker-compose.yml -f docker-compose.ghcr.yml up -d
#
# Images: ghcr.io/tajbrains/db-manager-{backend,frontend}

services:
  backend:
    image: ghcr.io/tajbrains/db-manager-backend:${MDCP_RELEASE_TAG:-latest}
    build: !reset null

  frontend:
    image: ghcr.io/tajbrains/db-manager-frontend:${MDCP_RELEASE_TAG:-latest}
    build: !reset null
